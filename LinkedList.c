#include <stdio.h>
#include <stdlib.h>



struct LinkedlistNode {
    char data;
    struct Node* next;
};


// create new node


struct LinkedlistNode* create(char data) {
    struct LinkedlistNode* newNode = (struct Node*)malloc(sizeof(struct Node));
    newNode->data = data;
    newNode->next = NULL;
    return newNode;
}


void inserthead(struct LinkedlistNode** head,char data){

    struct LinkedlistNode* newNode= create(data);
    newNode->next =*head;
    *head=newNode;



}

void displayLinkedList(struct Node* head) {
    struct LinkedlistNode* current = head;
    while (current != NULL) {
        printf("%d ", current->data);
        current = current->next;
    }
    printf("\n");
}






































































































































































































// `funtion to insert start

void insertHead(struct LinkedlistNode** head, char data){

s
]


















































































































































































































}

